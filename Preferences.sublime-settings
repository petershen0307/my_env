// Settings in here override those in "Default/Preferences.sublime-settings",
// and are overridden in turn by syntax-specific settings.
// export GDK_DPI_SCALE=1.3 to scale window frame
// setting path: ~/.config/sublime-merge/Packages/User/Preferences.sublime-settings
{
        "font_size": 12,
        "ui_scale": 1.5,
}
